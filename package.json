{
  "name": "portfolio",
  "scripts": {
    "build-all": "rm -rf build && mkdir -p build/home build/projects build/contacts build/project && cp index.html build/ && cp -r contacts/* build/contacts/ && cd home && npm run build && cp -r build/* ../build/home/ && cd ../project && npm run build && cp -r dist/* ../build/project/ && cd ../projects && trunk build --release"
  }
}
