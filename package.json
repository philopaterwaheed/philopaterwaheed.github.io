{
  "name": "portfolio",
  "scripts": {
    "build-all": "rm -rf build && mkdir -p build/portfolio build/projects build/contacts build/project  build/skills && cp index.html build/ && cp -r contacts/* build/contacts/ && cp -r skills/* build/skills/ && cd portfolio && npm run build && cp -r build/* ../build/portfolio/ && cd ../project && npm run build && cp -r dist/* ../build/project/ && cd ../projects && trunk build --release"
  }
}
