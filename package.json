{
  "name": "portfolio",
  "scripts": {
    "build-all": "rm -rf build && mkdir -p build/home build/projects build/contacts && cp index.html build/ && cp -r contacts/* build/contacts/ && cd home && npm run build && cp -r build/* ../build/home/ && cd ../projects && trunk build --release"
  }
}
